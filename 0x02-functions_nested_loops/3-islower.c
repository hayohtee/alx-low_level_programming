#include "main.h"

/**
* _islower - checks if the character is lowercase
* @c: a character
* Return: 1 if the character is lower or 0 if it is not
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
