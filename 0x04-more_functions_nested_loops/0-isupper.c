#include "main.h"

/**
* _isupper - checks if the character is uppercase
* @c: a character
* Return: 1 if the character is lower or 0 if it is not
*/
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
