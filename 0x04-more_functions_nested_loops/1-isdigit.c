#include "main.h"

/**
* _isdigit - checks if the character is a digit
* @c: a character
* Return: 1 if the character is digit or 0 if it is not
*/
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
